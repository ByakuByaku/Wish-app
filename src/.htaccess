<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Remove index.php from URLs
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ server/index.php?uri=$1 [QSA,L]
</IfModule>
